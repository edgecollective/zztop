# Host tooling

Python that drives the instrument over its serial command interface, orchestrates
sweeps, stores results, and fits models to them. Also holds the bench instrument
control used to cross-check the engine against an oscilloscope.

The firmware and this tooling are two halves of one measurement; neither is much use
alone.
